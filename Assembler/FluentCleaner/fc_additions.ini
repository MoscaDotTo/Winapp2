; fc_additions.ini (c) 2026 hazel ward
;
; This file is part of a collection of winapp2ool ini files that are used to generate winapp2.ini
; and thus is not intended to be used standalone or with any cleaning software
;
; Most entries in these files are incomplete and may not function as expected if used on their own
;
; winapp2ool uses this file to apply a FluentCleaner flavor to winapp2.ini
; This is done to reduce undercoverage
; Winapp2ool applies this file using Transmute with the Transmute mode set to Add
;
; Corrections are always applied in order:
; section removals -> key name removals -> key value removals -> section replacements
; -> key replacements -> additions (this file)
;
; Any keys defined here will be added to the section of the same name in winapp2.ini
; If you are simply adding to an existing entry, provide only the new keys
; If no such section exists, a new one will be created and added
; If you are adding an entirely new entry, be sure that it is complete (section keys, detect criteria, etc)

; FluentCleaner parses winapp2.ini entries as on by default, and makes downstream
; changes to ensure these web browser scaffolds are disabled by default. We absorb
; that here with [*Name:] rules that pattern-match on the scaffold name and insert a
; Default=False key into each matching entry, so new BrowserBuilder browsers are
; covered automatically.
;
; Each rule selects entries whose name ends with " <scaffold> *" (the label carries the
; bare scaffold name; the engine appends the "*"), then confirms the entry
; is a browser via the Match= keys. 

[*Name: Autofill Data & Search Engine Preferences]
Match1=Section=* Web Browser
Match2=Section=* Email Client
Match3=Section=* Internet Suite
Match4=Section=* ArtisBrowser
Default=False

[*Name: Bookmark Backups]
Match1=Section=* Web Browser
Match2=Section=* Email Client
Match3=Section=* Internet Suite
Match4=Section=* ArtisBrowser
Default=False

[*Name: Download History]
Match1=Section=* Web Browser
Match2=Section=* Email Client
Match3=Section=* Internet Suite
Match4=Section=* ArtisBrowser
Default=False

[*Name: Extension Cookies]
Match1=Section=* Web Browser
Match2=Section=* Email Client
Match3=Section=* Internet Suite
Match4=Section=* ArtisBrowser
Default=False

[*Name: Pinned Tabs]
Match1=Section=* Web Browser
Match2=Section=* Email Client
Match3=Section=* Internet Suite
Match4=Section=* ArtisBrowser
Default=False

[*Name: Saved Usernames & Passwords]
Match1=Section=* Web Browser
Match2=Section=* Email Client
Match3=Section=* Internet Suite
Match4=Section=* ArtisBrowser
Default=False

[*Name: Web Browsing Cookies]
Match1=Section=* Web Browser
Match2=Section=* Email Client
Match3=Section=* Internet Suite
Match4=Section=* ArtisBrowser
Default=False

[*Name: Web Browsing Session]
Match1=Section=* Web Browser
Match2=Section=* Email Client
Match3=Section=* Internet Suite
Match4=Section=* ArtisBrowser
Default=False

[*Name: Autocomplete History]
Match1=Section=* Web Browser
Match2=Section=* Email Client
Match3=Section=* Internet Suite
Match4=Section=* ArtisBrowser
Default=False

[*Name: Autofill Data]
Match1=Section=* Web Browser
Match2=Section=* Email Client
Match3=Section=* Internet Suite
Match4=Section=* ArtisBrowser
Default=False

[*Name: Web Browsing Session Backups]
Match1=Section=* Web Browser
Match2=Section=* Email Client
Match3=Section=* Internet Suite
Match4=Section=* ArtisBrowser
Default=False

; FluentCleaner adds warnings to some entries, these keys are taken
; directly from FluentCleaner's downstream copy of winapp2.ini
[Microsoft Office *]
Warning=Heads up: this also clears your pinned/favorite files in Word, Excel and PowerPoint. Office stores pinned items in the same "recent files" list, so they get wiped along with the recent-documents history. Leave this unchecked if you rely on Office favorites.

[Windows Recent Documents *]
Warning=Clears the recent documents list for all user accounts on this machine, not just the current user.

[Windows Shell - Folder/Dialog Box View Settings *]
Warning=Resets all Explorer folder view preferences (icon/details/column layouts), window sizes, positions, and File Open/Save dialog history. All custom folder layouts will be lost permanently.

[Windows Shell *]
Warning=Deletes Explorer\FileExts which stores your custom file type associations (e.g. which app opens .jpg or .png). After cleaning, Windows will no longer remember your chosen default apps for file types until you reassign them manually.

[Windows Start Menu *]
Warning=Resets the Start Menu program cache. The Start Menu may take a moment to rebuild on next login.

[Windows Taskbar *]
Warning=Clears Jump Lists (recently used files shown per app on the taskbar). Pinned items are safe, but all recent file history will be removed.
